import { initialState } from './driver.reducer';

describe('Driver Reducer', () => {
  describe('an unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

    });
  });
});
